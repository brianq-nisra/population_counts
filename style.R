titles <- createStyle(fontSize = 14,
                      textDecoration = "bold")

bold <- createStyle(textDecoration = "bold")

right_bold <- createStyle(textDecoration = "bold",
                          halign = "right")

numbers <- createStyle(numFmt = "#,##0",
                       halign = "right")
